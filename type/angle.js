/* @flow */

import {Int, Float} from "./core"

// Convert turns to radians. One turn is equal to 360°.
export type turns = (n:Float) => Float

// Convert degrees to radians.
export type degrees = (n:Float) => Float
